# Pyramid Blur単体テスト

`ComputePyramidBlurPass`の既定Level、設定値、縮小・拡大順序、計測範囲を実GPUなしで確認する。

- 1/2、1/4、1/8、1/16へ連続縮小する。
- 1/16から1/8、1/4、1/2、フル解像度へ順番に拡大する。
- `filterRadius`の既定値は1.0で、0.25から3.0までを受け付ける。
- 先頭の時刻は最初の縮小処理、末尾の時刻はフル解像度への拡大処理へ渡す。
- 線形HDR色を範囲制限せず、9点テントフィルターで拡大する。
